/**
 * Merge in locale
 * mi sposto sul branch a cui devo fare il merge
 * quindi mi sposto su master 
 * git checkout master
 * git merge fabian 
 * git push -u origin develop (git push)
 *
 * puoi fare solo git push se il branch è stato creato in remoto, cioè direttamente dal repository
 * 
 * Se invece il branch è stato creato in locale
 * altrimenti se fai solo git push, ti chiederà sempre di fare
 * git push --set-upstream origin fabian 
 * ma se fai
 * git push --set-upstream origin fabian
 * le prossime volte ti basterà fare git push
 * 
 */